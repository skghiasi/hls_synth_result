// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xdocompute.h"

extern XDocompute_Config XDocompute_ConfigTable[];

XDocompute_Config *XDocompute_LookupConfig(u16 DeviceId) {
	XDocompute_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XDOCOMPUTE_NUM_INSTANCES; Index++) {
		if (XDocompute_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XDocompute_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XDocompute_Initialize(XDocompute *InstancePtr, u16 DeviceId) {
	XDocompute_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XDocompute_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XDocompute_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

