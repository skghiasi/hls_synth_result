// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XDOCOMPUTE_H
#define XDOCOMPUTE_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xdocompute_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Control_BaseAddress;
} XDocompute_Config;
#endif

typedef struct {
    u32 Control_BaseAddress;
    u32 IsReady;
} XDocompute;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XDocompute_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XDocompute_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XDocompute_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XDocompute_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XDocompute_Initialize(XDocompute *InstancePtr, u16 DeviceId);
XDocompute_Config* XDocompute_LookupConfig(u16 DeviceId);
int XDocompute_CfgInitialize(XDocompute *InstancePtr, XDocompute_Config *ConfigPtr);
#else
int XDocompute_Initialize(XDocompute *InstancePtr, const char* InstanceName);
int XDocompute_Release(XDocompute *InstancePtr);
#endif

void XDocompute_Start(XDocompute *InstancePtr);
u32 XDocompute_IsDone(XDocompute *InstancePtr);
u32 XDocompute_IsIdle(XDocompute *InstancePtr);
u32 XDocompute_IsReady(XDocompute *InstancePtr);
void XDocompute_EnableAutoRestart(XDocompute *InstancePtr);
void XDocompute_DisableAutoRestart(XDocompute *InstancePtr);


void XDocompute_InterruptGlobalEnable(XDocompute *InstancePtr);
void XDocompute_InterruptGlobalDisable(XDocompute *InstancePtr);
void XDocompute_InterruptEnable(XDocompute *InstancePtr, u32 Mask);
void XDocompute_InterruptDisable(XDocompute *InstancePtr, u32 Mask);
void XDocompute_InterruptClear(XDocompute *InstancePtr, u32 Mask);
u32 XDocompute_InterruptGetEnabled(XDocompute *InstancePtr);
u32 XDocompute_InterruptGetStatus(XDocompute *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
